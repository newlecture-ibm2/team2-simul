spring:
  application:
    name: simul-backend
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

server:
  port: 8080

# Google Vision API 키 경로는 환경변수 GOOGLE_APPLICATION_CREDENTIALS 로 설정
# 예: set GOOGLE_APPLICATION_CREDENTIALS=C:\secrets\google-vision-key.json
